# 博客项目约定

- 为博客选择图片时，只使用 `source/images/myimge/` 中已有的图片。
- Markdown 和 Front Matter 中使用对应站点路径 `/images/myimge/<文件名>`。
- 每个图片文件在整个博客的配置、Front Matter 和正文中最多引用一次；不得把同一张图片重复用作 banner、cover 或正文插图。
- 可用图片不足时，省略图片字段或正文插图，不得重复使用，也不得改用其他目录的图片。
- 未经用户明确同意，不生成与文章无关的新图片，不从 `source/images/` 的其他子目录选图，也不删除现有图片文件。
- 用户提供原理图 PDF、参考文章 PDF 或其他项目资料用于写作时，可以从中截取与文章内容直接相关的页面或局部电路，保存到 `source/images/myimge/` 后用于文章；这类资料截图属于写作流程的一部分，不需要再次确认。
- 从 PDF 提取的每张图片使用唯一、能说明内容的文件名，并且在整个博客中只引用一次。

## PDF 和原理图配图

- 写作前先阅读用户提供的 PDF，找出真正支撑文章主线的框图、关键电路、PCB、波形、参数表或测试结果。
- 优先截取局部内容，不直接贴一整页包含大量无关信息的 PDF。截图应保留器件编号、网络名、坐标或必要标注，确保读者能够看清。
- 图片放在第一次讲到对应内容的段落附近。图片后面的文字要说明：图中是什么、它在系统中的作用、为什么这样设计、哪些结论已经确定、哪些仍待验证。
- 不使用“从图里可以看到一堆器件”式说明，不把截图当作装饰，也不逐项抄写图中文字。
- 原理图较复杂时，先给系统级框图或总览，再截取少量关键局部。原则上只保留对理解文章有实际帮助的图片。
- 每张资料截图注明资料名称和页码；引用外部参考资料时，在文章末尾列出来源。不要大段复制受版权保护的文章正文，也不要截取与分析无关的整篇内容。
- 公司内部、客户或未公开项目资料默认不得公开。只有用户明确确认可以发布时才使用；否则只写通用原理或使用已经公开的资料，不在博客中暴露原理图、参数、项目名称和可识别信息。
- 如果 PDF 图片模糊、缺页或无法确认电路关系，应明确说明，不凭模糊截图补造技术结论。

# 技术博客写作规范

写技术博客时，默认文章是**面向读者公开发布的项目记录**，而不是写给项目作者本人看的分析报告。

## 1. 叙事视角

站在作者的视角写，而不是 AI 助手的视角。

默认使用“我”“这次设计”“这块板”“这个项目”等自然表达。

不要出现类似：

- “等你后面测到……”
- “后面你可以……”
- “目前给出的资料……”
- “从你提供的原理图来看……”
- “等你把结果告诉我……”
- “后续我可以帮你补充……”

这些表达会让文章像 AI 在向作者汇报，而不是作者在向读者介绍自己的项目。

应该改成类似：

- “后续实测时会重点关注……”
- “这一版暂时还没有实测数据。”
- “目前先记录设计思路，实测完成后再补充结果。”
- “这里预留了测试点，方便后续验证环路状态。”

## 2. 写给“不了解这个项目的读者”

不要假设读者已经知道项目背景。

第一次出现一个电路、器件或设计选择时，要自然解释：

**它是什么 → 为什么需要它 → 这里为什么这样设计。**

不要只是罗列观察结果。

例如不要写：

“原理图中存在 DC24VToDC310V、DC24VToDC-310V 和 DC24VToDC12V。”

更适合写成：

“整块电源从 24V 输入开始，一路生成控制电路需要的低压电源，另外两路分别升压得到 ±310V 高压母线。±310V 并不是最终输出，而是继续送入后级线性稳压器，得到更干净的 ±300V。”

优先让读者理解设计，而不是证明作者已经从原理图里发现了什么。

## 3. 不要写成“原理图识别报告”

技术博客的核心应该是**设计思路和工程取舍**，不是描述图片里有什么。

少用：

- “可以看到……”
- “从截图可以看出……”
- “原理图文字层显示……”
- “基本可以确认……”
- “这说明……”
- “网络名里还能看到……”

如果某个器件或网络值得写，直接解释它在设计中的作用。

只有存在不确定性时，才明确使用“推测”“暂未验证”等措辞。

## 4. 减少 AI 式总结和重复

不要每一节都重复：

“这一部分很重要。”
“后面需要重点关注。”
“这对后续调试非常重要。”
“这里可以看出……”

一个信息原则上只完整解释一次。

章节之间应该继续推进内容，而不是不断换一种说法总结上一节。

## 5. 少用机械的“三段式”和列表

不要为了结构化而强行把所有内容拆成：

“第一个重点……第二个重点……第三个重点……”

也不要大量使用：

“首先 / 其次 / 再次 / 最后”

简单事实可以直接写成自然段。

只有以下内容适合列表：

- 参数
- BOM / 器件
- 测试条件
- 调试步骤
- 多个真正并列的设计要求

设计原理、分析和项目经历优先使用连贯自然段。

## 6. 写出工程上的“为什么”

不要停留在“用了什么器件”。

尽量回答：

- 为什么选择这个拓扑？
- 为什么母线是 ±310V 而输出是 ±300V？
- 为什么这里值得增加一级线性稳压？
- 10V 压差带来了什么好处和代价？
- 为什么反馈节点需要远离功率回路？
- 为什么某个参数选择这个数量级？
- 这个设计最担心什么问题？

技术博客的价值主要来自这些设计判断，而不是把原理图上的文字重新描述一遍。

## 7. 区分事实、设计目标和待验证内容

不要把尚未测试的结论写成已经成立的事实。

明确区分：

**已经确定**
设计参数、拓扑、器件型号、PCB 连接关系等。

**设计目标**
例如希望降低纹波、改善动态响应。

**尚待验证**
例如实际纹波、环路稳定性、温升、SOA、负载瞬态表现。

对于没有数据支持的地方，可以直接写：

“这一点目前还没有实测数据，等板子调通后再补。”

不需要为了让文章显得完整而提前给出结论。

## 8. 保留项目进行中的真实状态

项目记录不需要假装所有问题都已经解决。

允许出现：

- 目前还没有测试
- 这个参数暂时是按仿真确定的
- 第一版 PCB 这里可能存在问题
- 这个设计选择后续还需要验证
- 实测结果和预期不一致
- 暂时还不知道原因

这种信息比泛泛的正确结论更有技术博客价值。

## 9. 使用自然的工程师语言

语言可以专业，但不要刻意正式。

避免：

“从这一目标倒推……”
“这一点基本坐实了……”
“实际调试时必须先过它……”
“控制环能不能先站稳……”
“这一层负责把目标值和反馈值拢到一起……”

除非作者本身就习惯这种表达。

优先使用准确、普通的工程语言：

“为了给后级 LDO 留出压差，母线电压设为 ±310V。”

比：

“±310V 并不是最终目标，而是在整个高压供电链路中承担承上启下作用的重要母线节点。”

更好。

## 10. 不要过度包装普通事实

如果一句话很简单，就简单说。

不要为了显得专业而扩写。

例如：

“LDO 输入为 310V，输出为 300V，因此功率管正常工作时承担约 10V 压差。”

已经足够。

不要继续写大量“这意味着……这体现了……这也说明了……”除非确实有新的技术信息。

## 11. 参数优先于形容词

尽可能使用：

“300kHz”
“6.8µH”
“±310V → ±300V”
“200W”
“10V dropout”

少使用：

“较高的开关频率”
“较大的功率”
“足够的余量”
“非常干净的电源”

能量化的地方优先量化。

## 12. 默认文章结构

根据项目实际内容灵活调整，不要求每篇完全一致。通常优先考虑：

**项目背景 → 设计目标 → 总体方案 → 关键电路 → PCB/布局 → 仿真 → 调试过程 → 实测结果 → 问题与修改 → 总结**

没有内容的章节直接省略。

不要为了套模板而生成没有实际信息的章节。

## 13. 修改已有文章时

如果任务是润色已有技术博客：

1. 保留原有技术事实和作者判断。
2. 不自行补充未经确认的参数或测试结果。
3. 删除 AI 与作者对话式表达。
4. 把“AI 对项目的分析”转换成“作者向读者讲项目”。
5. 合并重复内容。
6. 删除没有信息增量的总结句。
7. 补充必要的上下文，使第一次看到项目的人也能理解。
8. 不要为了润色而把文章长度明显膨胀。
9. 如果原文本身存在技术疑点，单独指出，不要偷偷修改事实。
10. 最终文章应该读起来像工程师整理自己的设计与调试过程，而不是 AI 阅读原理图后生成的一份分析报告。

## 最终自检

完成后检查：

> 如果删掉图片和上下文，一个第一次看到这个项目的人是否仍然知道“这是做什么的、为什么这么设计、现在做到哪一步”？

以及：

> 文章里有没有任何一句话听起来像 AI 正在对项目作者说话？

如果有，继续修改。

# 长期写作流程

用户提供项目思路、工作记录、PDF、原理图、截图或测量数据后，不立即生成文章正文。默认按以下阶段推进：

1. 整理已经确定的事实、设计目标、作者判断、待验证内容、技术疑点和缺少的资料。
2. 必要时集中提出最多三个会实质影响文章方向的问题。
3. 只输出“文章结构卡”，包括文章解决的问题、目标读者、读者收益、章节顺序、每节核心信息、证据与图片安排、待补数据和不应写入的内容。
4. 与用户确认标题、摘要、开头切入方式、结尾结论、分类、标签和 PDF 截图清单。
5. 只有用户明确表示“开始写正文”或同等意思后，才生成或修改完整文章。
6. 发布前检查文章是否能帮助读者完成一次设计判断、测试或问题排查，并检查公司资料、客户信息和版权边界。

文章结构卡默认使用以下格式：

```text
文章要解决的问题：
目标读者：
读者看完能得到什么：
当前已经确定：
尚待验证：
建议章节：
每节核心信息：
准备使用的图片和数据：
还需要补充的资料：
不写入文章的内容：
```

每篇文章尽量只承担一种主要任务：项目记录、问题排查或原理学习。项目记录回答“做什么、为什么这样设计、目前到哪一步”；问题排查回答“出现什么现象、怎样定位、如何复测”；原理学习回答“一个技术为什么这样工作、关键参数和适用边界是什么”。

# 作者个人表达习惯

参考作者已有的培训材料，文章默认保留以下表达特征：

- 从一个实际需求或自己曾经没有理解的问题切入，再逐步补齐概念，不使用百科全书式开场。
- 新概念优先按“简单定义 → 直观解释 → 数学表达 → 图或例子 → 对当前问题的作用”展开。
- 公式不是装饰。先说明式子描述的物理关系，再给公式；推导应保留关键中间步骤，并在推导后用普通语言解释结果。
- 喜欢使用“为什么”“此时会怎样”“需要满足什么条件”推进内容。可以保留这种提问式过渡，但不要连续堆叠问句。
- 图和框图用于帮助理解信号流向、电流路径和前后因果。正文应紧接着解释图中最关键的关系，不逐项复述图中文字。
- 语言保持直接、朴素，允许使用“简单地说”“这里需要注意”“以……为例”等自然表达，不刻意包装成论文语言。
- 原理部分重视从定义推到结论。例如先写传递函数，再得到幅频和相频特性，最后讨论它对环路稳定性的影响。
- 小结只保留本节真正得到的结论、适用条件和仍未理解或待验证的地方，不泛泛重复正文。

参考旧稿的表达习惯不等于保留旧稿的错误。改写时应主动修正错别字、单位大小写、术语误用、公式下标、缺失条件和不成立的技术结论；如果修正会改变作者原本判断，应单独说明，而不是静默改写。

## MATLAB 与 Simulink 配图

- 可以使用 MATLAB 生成文章需要的时域波形、频谱、幅频特性、相频特性、波特图、极零图、阶跃响应和参数对比图；需要表达系统模块关系或动态模型时可以使用 Simulink。
- 仿真图必须服务于文章中的具体问题，不为了丰富版面生成无关曲线。原理学习文章应优先使用简洁、参数可解释的模型。
- 图片保存到 `source/images/myimge/` 下对应文章的独立目录，并遵守全站图片不重复使用的规则。
- 同时保留生成图片的 `.m` 脚本或 Simulink 模型，并在脚本中写明传递函数、元件参数、频率范围和单位，确保结果可以重新生成。
- 图中坐标轴、单位、图例、转折频率和关键参数必须标清。文章需要区分理论示意、软件仿真和硬件实测，不把仿真曲线写成实测结果。
- 使用 Simulink 前先判断普通 MATLAB 脚本和 Control System Toolbox 是否已经足够；只有模块连接或动态过程确实更容易用模型表达时才建立 `.slx` 文件。
